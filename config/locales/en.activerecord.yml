en:
  activerecord:
    attributes:
      repository/check:
        aasm_state: Status
        aasm_state/planed: Planed
        aasm_state/cloning: Cloning
        aasm_state/checking: Checking
        aasm_state/failed: Failed
        aasm_state/finished: Finished
        commit_id: Commit ID
        created_at: Create date
        offense: Offenses count
        passed: Check passed
      repository:
        created_at: Create date
        full_name: Full name
        github_id: Github repository
        language: Language
        name: Title
        updated_at: Update date
    errors:
      models:
        repository:
          attributes:
            github_id:
              taken: has already been added to your project!
    models:
      user: User
      repository: repository
